import 'package:flutter/material.dart';
import 'package:flutter/widgets.dart';

class BoardSpace extends StatelessWidget {
  const BoardSpace({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder();
  }
}
