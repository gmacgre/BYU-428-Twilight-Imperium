import 'package:flutter/material.dart';

class MovementCommandWidget extends StatelessWidget {
  const MovementCommandWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}