import 'package:flutter/material.dart';

class ProductionCommandWidget extends StatelessWidget {
  const ProductionCommandWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}